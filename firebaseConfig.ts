// firebaseConfig.ts

// 1. Go to https://firebase.google.com/ and create a new project.
// 2. In your project, go to Project Settings (gear icon) -> Your apps -> Web app.
// 3. Register a new web app. Firebase will give you a configuration object.
// 4. Copy the values from that object and paste them here.
// 5. In the Firebase console, go to "Firestore Database" and create a database in "Production mode".

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_AUTH_DOMAIN",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_STORAGE_BUCKET",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID"
};
